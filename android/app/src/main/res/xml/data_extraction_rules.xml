<?xml version="1.0" encoding="utf-8"?>
<!--
    Nothing in this app may leave the device through backup or transfer: the
    Room database is SQLCipher-encrypted with a key held in the Android
    keystore, so a restored copy would be unreadable, and the token store holds
    live session credentials.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
